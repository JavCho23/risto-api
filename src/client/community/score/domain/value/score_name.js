const RawString = require("../../../../../shared/domain/value/raw_string");

class ScoreName extends RawString {
    constructor(value) {
        super(value);
    }
}

module.exports = ScoreName;
