const RawString = require("../../../../../shared/domain/value/raw_string");

class MealDescription extends RawString {
  constructor(value) {
    super(value);
  }
}

module.exports = MealDescription;
